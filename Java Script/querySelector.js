function public() {
    const el = document.querySelector("#prompt"); // To select a specific element, we add a selector inside parentheses. For example the p tag: *.QuerySelector("p")* 
    //0querySelector only retreeves the first element

    el.innerHTML = "Website published!";
    document.get
}